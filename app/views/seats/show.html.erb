<div class="login-form">
	<p id="notice"><%= notice %></p>

	<p>
	  <strong>Name:</strong>
	  <%= @seat.name %>
	</p>

	<%= link_to 'Edit', edit_seat_path(@seat) %> |
	<%= link_to 'Back', seats_path %>
</div>
